The script is a program for working with stars, in order to use it, you need to open the config.ini file and
fill in all the lines correctly, and then run main.py, after which a .csv file will appear in the working catalog,
the name of which will be the current time